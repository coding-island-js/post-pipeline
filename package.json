{
  "name": "post-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Post-Production Workflow Tracker — Netlify + Airtable demo (Product Owner / M&E portfolio piece)",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod --dir=public"
  }
}
